# Used for local development. Sync changes except for the theme between this
# file and _config.yml.
theme: minimal-mistakes-jekyll
title: Creative Coding Lab

exclude:
  - README.md

plugins:
  - jekyll-paginate
  - jekyll-sitemap
  - jekyll-gist
  - jekyll-feed
  - jemoji
  - jekyll-include-cache
